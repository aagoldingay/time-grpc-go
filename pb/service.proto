syntax = "proto3";

package pb;

import "google/protobuf/timestamp.proto";

// TimeRecord service definition
service TimeRecord {
	// Sends a time request to the server to initiate a new job record
	rpc InitiateTimer (NewTimeRequest) returns (Confirmation) {}

	// Sends a time request to the server to complete a job request
	rpc CompleteTimer (CompleteRequest) returns (Confirmation) {}
}

enum JobStatus {
	NONE = 0;
	NEW = 1;
	STARTED = 2;
	PAUSED = 3;
	FINISHED = 4;
}

enum Error {
	NIL = 0;
	OK = 200;
	CREATED = 201;
	ACCEPTED = 202;
	BADREQUEST = 400;
	NOTFOUND = 404;
}

message Confirmation {
	int32 jobID = 1;
	JobStatus jobStatus = 2;
	Error error = 3;
}

message CompleteRequest {
	int32 jobID = 1;
	google.protobuf.Timestamp timer = 2;
}

message NewTimeRequest {
	google.protobuf.Timestamp timer = 2;
}